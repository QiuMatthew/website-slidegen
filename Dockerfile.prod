# Production Dockerfile - Lightweight version
FROM alpine:latest

WORKDIR /app

# Copy pre-built Go binary (built locally for linux/amd64)
COPY slidegen-server .
RUN chmod +x slidegen-server

# Create static directory for HTML files
RUN mkdir -p static

EXPOSE 8081

CMD ["./slidegen-server"]